#!/bin/bash

set -euxo pipefail

git tag temp-release-tag
git push origin temp-release-tag
curl -s https://api.github.com/repos/HGVSnomenclature/hgvs-nomenclature/releases/tags/temp-release-tag | jq -r .body
git tag -d temp-release-tag
git push origin --delete temp-release-tag
